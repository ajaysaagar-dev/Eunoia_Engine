#pragma once
// ============================================================================
// EnginePlatform::InputSystem — forwarding header
// The canonical implementation still lives at include/InputSystem.h (header-
// only). This header re-exports everything so code can include either path.
// ============================================================================

// Pull in the existing header-only implementation unchanged.
#include "../../include/InputSystem.h"
